# 疑似treeコマンドツール
## CUstomTkinterを使用した疑似treeコマンドツール
windowsにはないtreeコマンドを疑似的に行うツールです。

## 実行イメージ
### 実行画面
![実行画面](docs/01_tree_viewere(初期画面).png)
![実行画面](docs/02_tree_viewere(フォルダ選択).png)


## できること
- 選択したフォルダ内をtreeコマンド風に表示する  

## 使用技術
- Python
- Custom Tkinter
- Tkinter

## 環境
- Python 3.10 以上(pyファイル)
- Windows(exeファイル)


## 起動及び使用手順
main.exeファイルの実行
もしくはコマンドプロンプト(プロジェクトルート)で以下コマンドを実行  
python -m apps.treeviewer.main  

※python -m はPythonモジュールをスクリプト(実行用ファイル)として実行するためのコマンドラインオプション  

1. 対象ディレクトリを選択ボタンを押下  
2. ツリービューにディレクトリ内のフォルダ・ファイルが表示

## フォルダ構成
<details>
<summary>フォルダ構成(折り畳み)  </summary>

apps  
├treeviewer 
│		├─build(build及びdistはexeファイル作成時に自動生成)  
│   ├── dist  
│   │   └── main.exe  
│   ├── doc  
│   │   ├── 01_tree_viewere(初期画面).png  (実行時のスクリーンショット各種)   
│   │   ├── 02_tree_viewere(フォルダ選択).png  
│   │   ├── icon_01.clip(変換前iconファイル)  
│   │   └── icon_01.png(同上)  
│   ├── icon_01.ico  
│   ├── main.py  
│   ├── make_exe.bat  
│   └── README.md  
common   
└─共通処理用ディレクトリ  

</details>

## 簡易設計
<details>
<summary>簡易設計(折り畳み)  </summary>

main.py  
	∟__init__(初期化、frame生成)  
	∟ create_main_frame(フレーム内の要素を生成)  
	∟ select_dir(ディレクトリ選択の処理)  
	∟ display_tree(treeviewへの表示処理)  
	∟ clear(選択状態・表示状態のクリア)  
	∟ change_mode(サイドメニュー下部にあるモードチェンジ処理)  

</details>

## 簡易テスト
### ■正常系
1. 1つ目のファイル選択→セグメントボタンの表示が1つ目のファイル克、ツリービューにテキスト内容が表示される  
2. 2つ目のファイル選択→セグメントボタンの表示が2つ目のファイル克、ツリービューにテキスト内容が表示される  
3. 1.2.を実行後、差分部分が薄い赤色で表示される  
4. 1.2を実行後セグメントボタンによるツリービューの表示の切り替えが正常に実行される
5. 1.2.を実行後、差分のみ表示ボタンを押下し、ツリービューの表示が差分のみとなる
6. 6.を実行後、差分のみ表示ボタンがすべて表示ボタンに切り替わる → すべて表示ボタン押下で差分以外も表示される

### ■異常系
1. ファイルを選択していない状態で差分のみ表示ボタン押下 → エラー出力

## version履歴
- v1.0.0(2026-04-28)  
	初回リリース  

## 備考
本ツールは個人開発アプリです。  

## 今後の改善
ファイル選択のクリア実装  
ファイル出力の実装  